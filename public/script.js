document.getElementById('surveyForm').addEventListener('submit', async (e) => { e.preventDefault(); const formData = { name: document.getElementById('name').value, email: document.getElementById('email').value, satisfaction: document.getElementById('satisfaction').value, comment: document.getElementById('comment').value }; try { const response = await fetch('/api/responses', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(formData) }); if (response.ok) { document.getElementById('message').innerHTML = '<p class="success">ご���答ありがとうございます！</p>'; document.getElementById('surveyForm').reset(); loadResults(); } else { document.getElementById('message').innerHTML = '<p class="error">送信に失敗しました。</p>'; } } catch (error) { console.error('Error:', error); document.getElementById('message').innerHTML = '<p class="error">エラーが発生しました。</p>'; } }); async function loadResults() { try { const response = await fetch('/api/responses'); const data = await response.json(); const resultsDiv = document.getElementById('results'); resultsDiv.innerHTML = ''; if (data.length === 0) { resultsDiv.innerHTML = '<p>まだ回答がありません。</p>'; return; } const satisfactionCount = {}; data.forEach(item => { satisfactionCount[item.satisfaction] = (satisfactionCount[item.satisfaction] || 0) + 1; }); const labels = { 'very-satisfied': '非常に満足', 'satisfied': '満足', 'neutral': 'どちらでもない', 'unsatisfied': '不満', 'very-unsatisfied': '非常に不満' }; resultsDiv.innerHTML += '<h3>満足度集計</h3>'; for (const [key, count] of Object.entries(satisfactionCount)) { resultsDiv.innerHTML += `<div class="result-item"><p><strong>${labels[key]}:</strong> ${count}件</p></div>`; } resultsDiv.innerHTML += `<h3>総回答数: ${data.length}件</h3>`; } catch (error) { console.error('Error loading results:', error); } } loadResults();